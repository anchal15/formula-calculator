// config.js
const config = {
    enableSyntaxHighlighting: true,  // Feature flag for syntax highlighting
    enableFormulaSaving: true,        // Feature flag for saving formulas
    userPermissions: {
      canSaveFormulas: true,          // Permission for saving formulas
      canUseAdvancedMath: true        // Permission for advanced math functions
    }
  };
  
  export default config;
  